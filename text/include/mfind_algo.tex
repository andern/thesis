%\KwIn{($\mathcal{M}_l$, $\mathcal{Q}_k$)}
\SetAlgoVlined
\If{$\mathcal{M}_l \subseteq \mathcal{Z}_k$}{
    \KwRet{true}
}
\ForEach{child vertex $v_i$ of $v_k$}{
    \If{$\mathcal{M}_i \subseteq \mathcal{M}_l$}{
        $v_* \leftarrow v_i$ \\
        $a \leftarrow$ \texttt{mfind}($\mathcal{M}_l$, $v_i$, $v_*$) \\
        \If{$a$}{
            \KwRet{true}
        }
    }
}
\KwRet{false}
